# 🌐 نشر الموقع على GitHub Pages — الخطوات اللي عليك انت

> ✅ **اللي اتعلب مني دلوقتي:**
> - عملت merge لفرع `develop` جوه `main` (كل شغلنا الجديد اتضاف لـ main)
> - عملت ملف `.github/workflows/pages.yml` — ده اللي بيخلي الموقع **يتحدث تلقائيًا**
>   كل ما تعمل push جديد على `main` من غير ما تعمل أي حاجة تاني

---

## 👉 خطوتين بس عليك انت تعملهم:

### 1) ارفع الفرع main لـ GitHub

افتح **Terminal / Git Bash / VS Code Terminal** جوه مشروع الـ portfolio واكتب:

```bash
git push origin main
```

(لو GitHub طلب منك login — اعمل login مرة واحدة وبعدها هيظبط)

---

### 2) فعّل GitHub Pages من إعدادات الريبو (مرة واحدة بس)

1. افتح الريبو بتاعك: **https://github.com/Korya0/portfolio**
2. ادخل على **Settings** (تبويب الـ ⚙️ فوق)
3. من الشريط الجانبي اختار **Pages**
4. تحت **Build and deployment**:
   - **Source** → اختار **GitHub Actions** (مش "Deploy from a branch")
5. سيبه كده — الـ Workflow اللي عملته هيشتغل لوحده

---

## 🎯 بعد الخطوتين

- الموقع هيبقى متاح على: **https://korya0.github.io/portfolio/**
- كل ما تعمل `git push` جديد على `main` → الـ site بيتحدث **تلقائيًا** بعد دقيقة تقريبًا
- تقدر تتابع حالة النشر من تبويب **Actions** في الريبو

---

## 🧪 التجربة (اختياري)

بعد ما تخلّص الخطوتين، جرّب تعديل أي حاجة صغيرة في `index.html` (كلمة أي كلام)،
وcommit + push على `main`، وافتح الـ Actions تشوف الـ Workflow شغال،
وبعدها افتح رابط الموقع وشوف التعديل ظهر.

---

*الملف ده للمراجعة فقط — مش مطلوب منك تعديله أو مسحه.*
